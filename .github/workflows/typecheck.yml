name: typecheck

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ty:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
      - run: uvx ty@0.0.79 check --python-version 3.12 --output-format github monads
